clear variables
close all

fname = 'src/model-comparison-XYZ.csv';
data = importfile(fname);

% colors = [122,164,87; ...
% 164,108,183;
% 203,106,73];

colors = [186,190,158; ...
216,176,192; ...
152,193,208];


x = data( data{:,2}==1 , : );
y = data( data{:,2}==2 , : );
z = data( data{:,2}==3 , : );

labels = categorical;
labels(1) = 'Kashima';
labels(2) = 'Mizusawa';
labels(3) = 'Usuda';
labels(4) = 'Okushiri';
labels(5) = 'Kashiwazaki';
labels(6) = 'Kushiro';
labels(7) = 'Medan';
labels(8) = 'PulauSimuk';
labels(9) = 'Santiago';
labels(10) = 'LosAngeles';
labels(11) = 'Concepcion';

figure
b=bar(labels, [ x.rms, y.rms, z.rms ], 'FaceColor','flat', 'EdgeColor','none');
for i = 1:3
   b(i).FaceColor = colors(i, :)./255;
end

m = mean( data.rms );

hold on
plot(xlim, [ m , m ], 'r')

set(gca, 'YGrid', 'on', 'XGrid', 'off')
ylabel('RMS [mm]')
legend({'X','Y','Z'})


function modelcomparisonXYZ = importfile(filename, dataLines)
%IMPORTFILE Import data from a text file
%  MODELCOMPARISONXYZ = IMPORTFILE(FILENAME) reads data from text file
%  FILENAME for the default selection.  Returns the data as a table.
%
%  MODELCOMPARISONXYZ = IMPORTFILE(FILE, DATALINES) reads data for the
%  specified row interval(s) of text file FILENAME. Specify DATALINES as
%  a positive scalar integer or a N-by-2 array of positive scalar
%  integers for dis-contiguous row intervals.
%
%  Example:
%  modelcomparisonXYZ = importfile("C:\Users\david\Documents\ma2020\ma2020-dgfi-dw\src\model-comparison-XYZ.csv", [3, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 03-Oct-2020 23:19:27

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [3, Inf];
end

%% Setup the Import Options
opts = delimitedTextImportOptions("NumVariables", 6);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["station", "coordinate", "rms15w", "rms1y", "rms2y", "rms"];
opts.VariableTypes = ["categorical", "double", "double", "double", "double", "double"];
opts = setvaropts(opts, 1, "EmptyFieldRule", "auto");
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Import the data
modelcomparisonXYZ = readtable(filename, opts);

end
